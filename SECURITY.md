# Security

This lab is designed to run without secrets or external APIs.

- Do not add real credentials to fixtures.
- Keep reports synthetic.
- Treat tool definitions as an allowlist.
- Add regression tests for new tool execution paths.
